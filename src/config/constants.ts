/**
 * Global configuration constants for the application.
 */

// Interval in milliseconds for background auto-refresh across all pages
// Change this single value to adjust refresh rate globally for all views.
export const AUTO_REFRESH_INTERVAL_MS = 1 * 60 * 1000; // 2 minutes
